# PRD — 오이(52)지마켓

> **제품 요구사항 정의서 (Product Requirements Document)**
> 최종 갱신: 2026-09-02 · 운영 URL: https://52gs.netlify.app

---

## 1. 제품 개요

### 한 줄 정의
GS 계열사 임직원이 사내에서만 쓰는 **중고거래 · 나눔 마켓 PWA**.

### 왜 만들었나
사무실에서 안 쓰는 사무용품·전산소모품·가구가 그냥 버려지거나 창고에 쌓인다.
당근마켓 같은 외부 서비스는 사내 물품을 올리기 부적절하고, 사내 게시판은
사진·채팅·거래상태 관리가 안 된다. **사내 신원이 보장된 사람끼리만** 거래하는
가벼운 마켓이 필요했다.

### 핵심 가치
| 가치 | 설명 |
|---|---|
| **신뢰** | GS 계열사 이메일 OTP 인증을 통과한 사람만 입장. 익명 거래 아님 |
| **속도** | 매물·채팅이 실시간 반영 (Firestore 구독). 새로고침 불필요 |
| **가벼움** | 앱 설치 없이 웹으로 즉시. 원하면 PWA/APK 설치 가능 |
| **재미** | 나눔에 포인트를 주고 랭킹으로 노출해 선순환 유도 |

---

## 2. 사용자

### 페르소나
| 이름 | 상황 | 니즈 |
|---|---|---|
| **정리하는 김대리** | 자리 이동으로 모니터암·책장이 남음 | 버리기 아까움. 필요한 사람 찾아 나눠주고 싶음 |
| **아끼는 이과장** | 신규 부서 세팅 중 비품 부족 | 새로 사기 전에 사내에 남는 게 있는지 보고 싶음 |
| **모으는 박주임** | 나눔을 자주 함 | 기여가 눈에 보이면 좋겠음 (포인트·랭킹) |

### 접근 권한
로그인 가능한 도메인은 **11개 GS 계열사**로 제한한다.

| 회사 | 도메인 |
|---|---|
| (주)GS | `gs.co.kr` |
| GS리테일 | `gsretail.com` |
| GS건설 | `gsenc.com` |
| GS칼텍스 | `gscaltex.com` |
| GS파워 | `gspower.co.kr` |
| GS EPS | `gseps.com` |
| GS E&R | `gsenr.com` |
| GS글로벌 | `gsg.co.kr` |
| GS엔텍 | `gsentec.com` |
| GS스포츠 | `gssports.co.kr` |
| 파르나스호텔 | `parnas.co.kr` |

> 도메인은 **완전 일치**로만 검사한다. `gsretail.com.evil.com` 같은 우회를 막기 위해
> 부분 문자열 비교를 쓰지 않는다. 검증은 **클라이언트와 서버 양쪽**에서 한다
> (클라이언트 검사는 UX용일 뿐, 실제 차단은 서버가 한다).

---

## 3. 기능 요구사항

### 3.1 로그인 (이메일 OTP)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| A-1 | 계열사 이메일 입력 → 6자리 인증번호 메일 발송 | 필수 |
| A-2 | 로그인 첫 화면에 **가입 가능한 11개사 목록을 항상 표시** | 필수 |
| A-3 | 입력한 이메일의 계열사명을 실시간 표시 (`✓ GS리테일 계정으로 확인됐어요`) | 권장 |
| A-4 | 인증번호 6칸 입력. 자동 포커스 이동, 붙여넣기 시 6칸 자동 분배 | 필수 |
| A-5 | 모바일 메일 앱의 OTP 자동완성 지원 (`autocomplete="one-time-code"`) | 권장 |
| A-6 | 유효시간 5분. 남은 시간 표시 | 필수 |
| A-7 | 인증 성공 시 Firebase 커스텀 토큰을 함께 발급받아 실시간 DB 접근 권한 획득 | 필수 |

**비고 — 왜 OTP인가:** 사내 SSO 연동은 각 사 IT 승인이 필요해 현실적으로 불가능했다.
"그 회사 메일함에 접근할 수 있는 사람 = 그 회사 직원"이라는 최소 가정으로 신원을 보증한다.

### 3.2 매물

| ID | 요구사항 | 우선순위 |
|---|---|---|
| P-1 | 목록 조회. 카테고리·거래방식·검색어 필터 | 필수 |
| P-2 | **기본 보기 = 작게 보기(테이블)**. 카드형 토글 제공 | 필수 |
| P-3 | 등록: 제목·가격·설명·위치·카테고리·거래방식(나눔/판매) | 필수 |
| P-4 | 사진 **최대 3장**. 업로드 시 WebP 변환 후 저장 | 필수 |
| P-5 | 업로드 진행률을 **0~100%** 로 표시. 목록에 사진이 실제로 보일 때까지 진행 표시 유지 | 필수 |
| P-6 | 사진 로딩 실패 시 **사용자 모르게 자동 재시도** (최대 4회, 지수 백오프) | 필수 |
| P-7 | 상세 화면에서 3장 모두 열람 | 필수 |
| P-8 | 본인 매물만 수정·삭제. 삭제는 실제 삭제가 아닌 `status='삭제'` (이력 보존) | 필수 |
| P-9 | 찜하기 | 권장 |

**비고 — 왜 3장인가:** Google Drive 저장 + Apps Script 업로드 경로라 장당 업로드 비용이 크다.
실사용에서 3장이면 상태를 충분히 전달할 수 있어 상한을 뒀다.

### 3.3 거래 완료

| ID | 요구사항 | 우선순위 |
|---|---|---|
| D-1 | **판매자만** 거래완료 처리할 수 있다 | 필수 |
| D-2 | 판매(유상)는 입금확인 단계를 거친다 (`입금대기` → `거래완료`) | 권장 |
| D-3 | 거래완료 시 포인트가 **영구 기록**된다 (매물을 지워도 남는다) | 필수 |

**비고 — 왜 판매자만인가 (중요):** 초기에는 구매자가 "나눔 받았어요"를 누르면 거래가
종료됐다. 그러자 구매자가 실수로 눌러 거래가 사라지는 일이 생겼다. 물건을 실제로
넘긴 사실을 아는 쪽은 판매자이므로, 완료 권한을 판매자로 옮겼다.

### 3.4 채팅

| ID | 요구사항 | 우선순위 |
|---|---|---|
| C-1 | 매물별 1:1 대화. **실시간** 송수신 | 필수 |
| C-2 | 안읽은 대화 수를 하단 탭에 배지로 표시 | 필수 |
| C-3 | 한 번 읽으면 **기기가 바뀌어도** 배지가 유지되지 않는다 (계정 기준 읽음) | 필수 |
| C-4 | 새 메시지 도착 시 모바일 푸시 알림 | 필수 |
| C-5 | 이미 읽은 대화는 **알림이 중복 발생하지 않는다** | 필수 |
| C-6 | 거래완료·삭제된 매물의 대화는 **종료 표시**하고 목록 하단으로 내린다 | 필수 |
| C-7 | 대화별로 **내 목록에서만 삭제** 가능 (상대 목록엔 유지) | 필수 |
| C-8 | **거래완료 대화 숨기기** 필터 | 필수 |

**비고 — C-7 설계 근거:** 한쪽이 지웠다고 대화를 실제 삭제하면 상대의 기록까지
사라진다. 그래서 `hiddenAt.{uid}` 에 숨긴 **시각**을 남기고, 그 이후 새 메시지가 오면
자동으로 다시 나타나게 했다. (불리언이 아니라 타임스탬프인 이유)

### 3.5 포인트 · 랭킹

| ID | 요구사항 | 우선순위 |
|---|---|---|
| R-1 | 나눔 **3점**, 판매 **2점** | 필수 |
| R-2 | 매물이 삭제돼도 포인트는 유지 | 필수 |
| R-3 | 한 매물당 **1회만** 적립 (중복 방지) | 필수 |
| R-4 | 내정보: 이달 / 누적 / 연도별(2026) 포인트 | 필수 |
| R-5 | 랭킹: **이달 · 연간 · 전체** 3개 탭 | 필수 |

**비고 — R-3 구현:** 포인트 문서의 **id 를 매물 id 로** 쓴다. 같은 매물을 두 번
적립하려 해도 같은 문서를 덮어쓸 뿐이라 점수가 불어나지 않는다 (멱등성).

### 3.6 알림

| ID | 요구사항 | 우선순위 |
|---|---|---|
| N-1 | 키워드 등록 → 일치하는 새 매물 등록 시 푸시 | 필수 |
| N-2 | 새 채팅 도착 시 푸시 | 필수 |
| N-3 | 웹 접속 중이면 토스트로 새 채팅 알림 | 권장 |
| N-4 | **알림 권한 팝업을 자동으로 띄우지 않는다** | 필수 |

**비고 — N-4:** 접속하자마자 브라우저 권한 팝업이 뜨는 게 방해된다는 피드백이 있었다.
설정을 끄는 것만으로는 부족해 **SDK 자체를 사용자가 알림 버튼을 누를 때 지연 로드**한다.

### 3.7 PWA

| ID | 요구사항 |
|---|---|
| W-1 | 홈 화면 설치 지원 (manifest + 서비스워커) |
| W-2 | PWABuilder 로 Android APK/AAB 패키징 |
| W-3 | Digital Asset Links (`/.well-known/assetlinks.json`) 호스팅 |
| W-4 | 오프라인 시에도 앱 껍데기는 열림 |

---

## 4. 비기능 요구사항

| 항목 | 목표 | 현재 |
|---|---|---|
| 매물 목록 첫 표시 | 2초 이내 | **0.33초** (Netlify CDN 캐시) |
| 사진 1장 표시 | 1초 이내 | **0.5초** (Drive 썸네일 직접 호출) |
| 채팅 왕복 | 1초 이내 | 실시간 구독 (로컬 즉시 반영) |
| 동시 사용자 | 수백 명 | Firestore 무료 티어 내 |
| 브라우저 | 모바일 Chrome/Safari 우선 | — |

> **성능 이력:** 초기에는 매물 목록에 35.3초가 걸렸다. 측정해보니 Apps Script
> 플랫폼 자체의 요청당 오버헤드(10~40초)가 원인이었고, 시트 읽기 코드는 문제가
> 아니었다. 그래서 코드를 최적화하는 대신 **Netlify CDN 프록시**를 앞에 두어
> 35.3초 → 0.33초로 줄였다.

---

## 5. 화면 구성

```
┌─ 로그인 (/login) ──────────── 계열사 목록 + 이메일 → OTP 6자리
│
├─ 홈 (/) ──────────────────── 매물 목록 · 필터 · 검색 · 보기전환
│    └─ 상세 시트 ─────────── 사진 3장 · 채팅 시작 · (판매자) 완료/수정/삭제
│
├─ 올리기 (/upload) ────────── 사진 3장 + 정보 입력 · 진행률 %
│
├─ 채팅 (/chats) ───────────── 대화 목록 · 완료 필터 · 개별 삭제
│
├─ 찜 (/jar) ───────────────── 찜한 매물
│
├─ 랭킹 (/ranking) ─────────── 이달 / 연간 / 전체
│
├─ 알림 (/noti) ────────────── 키워드 관리 · 알림 이력
│
├─ 내정보 (/me) ────────────── 프로필 · 포인트 · 내 매물 · 로그아웃
│
└─ 관리자 (/migrate) ───────── 시트→Firestore 이관 · 진단 · 포인트 소급
```

**헤더:** 우측 끝에 작은 원형 로그아웃 버튼 (닉네임 첫 글자 표시)
**하단 탭:** 홈 · 채팅(배지) · 올리기 · 찜 · 내정보

---

## 6. 범위 밖 (Non-goals)

- 실제 결제 연동 (계좌 정보 안내까지만)
- 배송 · 택배 연동
- 공개 인터넷 사용자 대상 서비스
- 관리자 대시보드 (통계·정산)
- 다국어

---

## 7. 알려진 제약

| 제약 | 이유 | 대응 |
|---|---|---|
| 사진은 Google Drive 에 저장 | Firebase Storage 는 Blaze(유료) 필요 | 기존 Apps Script 경로 유지 |
| 사진 업로드가 느림 | Apps Script 요청 오버헤드 | 진행률 표시 + 자동 재시도 |
| OTP 메일이 스팸함에 갈 수 있음 | Gmail 발신 | 안내 문구 |
| 서비스 계정 키 생성 불가 | 조직 정책 차단 | IAM `signJwt` 위임 서명 |
